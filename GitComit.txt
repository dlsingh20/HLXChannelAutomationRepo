Git CLI Command Line Interface
Download Git CLI and install

Move to Project Location E: enter
git init  Enter
git status Enter

git checkout -b devlop     to crea
git checkout -b feature/m1-f1.0

git add .
(In Chase one file git add testpage.java)
git status

git comitt -m "Din First Comitt"

git push origin feature/m1-f1.1

git pull origin feature/m1-f1.1